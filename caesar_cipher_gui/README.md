# Caesar Cipher GUI (Tkinter)

Stylish Tkinter app for encrypting/decrypting ASCII text using a Caesar cipher. File open/save + copy-to-clipboard included.

## Run

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py