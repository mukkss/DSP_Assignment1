# RSA Key Generator (CLI)

A simple command-line tool to generate RSA key pairs, save them as `.pem` files, and optionally view them in the terminal.

## 🛠️ Usage

```bash
python main.py [-s SIZE] [-o OUT_DIR] [-p PASSWORD] [--show {public,private}]
